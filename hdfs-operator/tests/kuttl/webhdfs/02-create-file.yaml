apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: kubectl cp ./webhdfs.sh webhdfs:/tmp
  - script: kubectl cp ./testdata.txt webhdfs:/tmp
  - script: kubectl exec webhdfs -- /tmp/webhdfs.sh create
  - script: kubectl exec webhdfs -- /tmp/webhdfs.sh ls
